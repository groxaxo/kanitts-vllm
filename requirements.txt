# Core dependencies for KaniTTS-vLLM
# Optimized for low VRAM usage (~2GB) while maintaining real-time performance

# FastAPI and web server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# PyTorch ecosystem
torch>=2.1.0
numpy>=1.24.0
scipy>=1.11.0

# Audio processing
nemo-toolkit[tts]==2.4.0

# vLLM for optimized inference
vllm>=0.11.0

# Transformers for model compatibility
transformers==4.57.1

# Optional: BitsAndBytes for quantization (currently disabled due to vLLM compatibility)
# bitsandbytes>=0.46.1

# Additional utilities
pydantic>=2.0.0
base64
json
io
asyncio
time
threading
concurrent.futures

# System dependencies (install via package manager):
# - ffmpeg (required by nemo-toolkit)
#   Ubuntu/Debian: sudo apt install ffmpeg
#   CentOS/RHEL: sudo yum install ffmpeg
